# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
modules:
  - type: dnf
    install:
      packages:
        - pipewire
        - wireplumber
        - qt5-qtwayland
        - qt6-qtwayland
        - xdg-user-dirs
